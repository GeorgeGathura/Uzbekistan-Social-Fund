@model IEnumerable<Uzbekistan_Social_Fund.Models.FundApplication>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BenefitTwoYearOld)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BenefitFamilyWithTeens)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BenefitLowIncomeFamily)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ApplicantId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BenefitTwoYearOld)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BenefitFamilyWithTeens)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BenefitLowIncomeFamily)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ApplicantId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
